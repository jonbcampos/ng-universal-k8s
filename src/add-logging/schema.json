{
    "$schema": "http://json-schema.org/schema",
    "id": "add-logging",
    "title": "A schematic to add k8s logging functionality to Angular Universal if it already exists.",
    "type": "object",
    "properties": {
        "project": {
            "type": "string",
            "description": "The name of the project.",
            "$default": {
                "$source": "projectName"
            }
        }
    },
    "additionalProperties": true
}